# 기본
# python==3.14.3

# 백엔드
fastapi>=0.110.0,<1.0.0
uvicorn>=0.29.0,<1.0.0
pydantic>=2.0.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# LLM & 벡터DB
google-genai>=1.0.0
openai>=1.40.0,<2.0.0
# chromadb 1.x: prebuilt wheels on Windows (0.6.x pulled chroma-hnswlib source build / MSVC)
chromadb>=1.0.0,<2.0.0
tiktoken>=0.7.0,<1.0.0

# PDF 파싱
pymupdf>=1.24.0,<2.0.0
docling>=2.0.0

# 위키 HTML 수집/파싱 (api-spec.md 섹션 9) — 순수 파이썬, 폐쇄망 설치 용이
beautifulsoup4>=4.12.0
httpx>=0.27.0

# Hybrid Search (api-spec.md 섹션 10) — BM25 키워드 검색 (순수 파이썬)
# 한국어 토큰화는 문자 bigram 사용 — kiwipiepy는 한글 사용자명 경로에서
# 네이티브 모델 로딩 실패+세그폴트로 제외 (TROUBLESHOOTING.md 2026-06-11)
rank_bm25>=0.2.2

# 테스트
pytest>=8.0.0
